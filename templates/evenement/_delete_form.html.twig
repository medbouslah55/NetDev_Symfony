<form method="post" action="{{ path('evenement_delete', {'idPub': evenement.idPub}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.idPub) }}">
    <button class="btn">Delete</button>
</form>
