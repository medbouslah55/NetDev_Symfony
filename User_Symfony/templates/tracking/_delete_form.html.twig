<form method="post" action="{{ path('tracking_delete', {'idTrack': tracking.idTrack}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tracking.idTrack) }}">
    <button class="btn">Delete</button>
</form>
